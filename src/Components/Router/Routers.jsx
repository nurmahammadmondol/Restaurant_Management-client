import React from 'react';
import { createBrowserRouter } from 'react-router-dom';

const Routers = createBrowserRouter([
  {
    path: '/',
    element: <div>Hello world web page create success!</div>,
  },
]);

export default Routers;
